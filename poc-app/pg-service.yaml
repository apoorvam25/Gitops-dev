apiVersion: v1
kind: Service
metadata:
  name: pg-db
  namespace: payment-prod
  annotations:
    argocd.argoproj.io/hook: PreSync
spec:
  selector:
    app: postgres            
  ports:
  - protocol: TCP
    port: 5432
    targetPort: 5432