apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
spec:
  accessModes:
    - ReadWriteOnce # This volume can only be mounted by one pod at a time
  resources:
    requests:
      storage: 1Gi # Ask for 1 GB of storage