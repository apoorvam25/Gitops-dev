apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-prod
  namespace: recovery-test
spec:
  replicas: 0
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - image: nginx:latest
        name: nginx